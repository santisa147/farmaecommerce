import { createTheme } from "@mui/material";
import { cyan } from "@mui/material/colors";
const theme = createTheme({
    palette:{
        primary:{main:cyan['A200']},
        secondary:{main:cyan[50]}
    }
})


export default theme